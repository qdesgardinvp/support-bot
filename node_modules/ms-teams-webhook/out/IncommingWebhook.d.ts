import { IncomingWebhookResult } from "./types";
/**
 * A client for Teams's Incoming Webhooks
 */
interface Payload {
    [key: string]: any;
}
export declare class IncomingWebhook {
    /**
     * The webhook URL
     */
    private url;
    /**
     * Axios HTTP client instance used by this client
     */
    private axios;
    constructor(url: string);
    /**
     * Send a notification to a conversation
     * @param message the message (object describing the message)
     */
    send(message: Payload): Promise<IncomingWebhookResult | undefined>;
    /**
     * Processes an HTTP response into an IncomingWebhookResult.
     */
    private buildResult;
}
export {};
//# sourceMappingURL=IncommingWebhook.d.ts.map